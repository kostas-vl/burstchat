/**
 * This interface contains all information that a user required to execute
 * voice calls through sip / webrtc.
 * @interface
 */
export interface SipCredentials {

    username: string;
    password: string;

}
